<UserControl x:Class="Lab02Mykhailenko.Views.AllPeople"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lab02Mykhailenko.Views"
             xmlns:model="clr-namespace:Lab02Mykhailenko.Models"
             xmlns:viewmodels="clr-namespace:Lab02Mykhailenko.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:PeopleViewModel}"
             mc:Ignorable="d" 
             xmlns:col="clr-namespace:System.Collections;assembly=mscorlib"
             d:DesignHeight="450" d:DesignWidth="1000" d:Background="Beige">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <DataGrid x:Name="peopleGrid" SelectedItem="{Binding Path=MyProperty}" Grid.Row="0" Grid.Column="0" AutoGenerateColumns="False" ItemsSource="{Binding Path=People}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Ім'я" >
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate >
                            <TextBox Text="{Binding Name, Mode=TwoWay}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Прізвище" >
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate >
                            <TextBox Text="{Binding Surname, Mode=TwoWay}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Email" >
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate >
                            <TextBox Text="{Binding Email, Mode=TwoWay}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Дата народження" >
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate >
                            <DatePicker SelectedDateFormat="Short" 
                  SelectedDate="{Binding Birthday, Mode=TwoWay}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Чи дорослий?" Binding="{Binding IsAdult}"/>
                <DataGridTextColumn Header="Західна астрологія:" Binding="{Binding SunSign}" />
                <DataGridTextColumn Header="Китайська астрологія:" Binding="{Binding ChineseSign}" />
                <DataGridTextColumn Header="Чи сьогодні народився?" Binding="{Binding IsBirthday}" />

                <DataGridTemplateColumn Header="Видалити" >
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate >
                            <Button Name="xml_btn_001" 
                                     Content="Видалити" 
                                     Command="{Binding Path=DataContext.DeletePersonCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button  Grid.Row="1" Margin="5" Grid.Column="1" VerticalAlignment="Center" Width="200" Height="30" Content="Додати персону" Command="{Binding AddPersonCommand}"/>

    </Grid>
</UserControl>
